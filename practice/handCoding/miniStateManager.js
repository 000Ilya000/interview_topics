// ✍️ HAND CODING — реализуй БЕЗ AI

//! Mini state manager: createStore(initialState) → { getState, setState, subscribe }

export function createStore(initialState) {
  // TODO: твоя реализация (как упрощённый Zustand/Redux)
  return {
    getState: () => {},
    setState: (partial) => {},
    subscribe: (listener) => () => {},
  };
}
