//! HTML / CSS / a11y — спрашивают в начале собеса

//! Семантика:
// article — самодостаточный контент, section — тематическая группа
// main (один), nav, aside, header, footer

//! Flex vs Grid:
// Flex — 1D layout, Grid — 2D
// justify-content / align-items vs grid-template-columns / gap

//! BFC / z-index:
// Stacking context: transform, opacity, position+z-index
// z-index работает только внутри своего context

//! a11y:
// Semantic HTML > ARIA, keyboard nav, focus trap, WCAG AA contrast 4.5:1

//! Мета: viewport, description (SEO), Open Graph (og:title, og:image)

export {};
